pipeline {
  agent { label 'master' }
  stages {
    stage('Build') {
            agent any
            steps {
                checkout scm
                sh 'date'
                 
            }
        }
  }
}
